#include <stdio.h>
#include <string.h>

int main()
{

    // // char s = 'a';
    // // char s[10] = "abascsca";
    // char s[101];
    // fgets(s, 101, stdin);
    // // scanf("%s") Bina Nusantara (Bina)

    // // strlen("Bina Nusantara") output : 14
    // for (int i = 0; i < 101; i++)
    // {
    //     if (s[i] != 'A' && s[i] != 'I' && s[i] != 'U' && s[i] != 'O' && s[i] != 'E')
    //     {
    //         printf("%c", s[i]);
    //     }
    // }

    // // boolean = true(1) / false(0)
    // // Good hill
    // int n, boolean = 1;
    // scanf("%d", &n); // 5

    // int arr[n]; // arr[5] = {99, 77, 55, 66, 44}
    // for (int i = 0; i < n; i++)
    // {
    //     // i = 0: arr[0] = 99
    //     // i = 1; arr[1] = 77
    //     // i = 2; arr[2] = 55
    //     // i = 3; arr[3] = 66
    //     // i = 4; arr[4] = 44
    //     scanf("%d", &arr[i]);
    // }

    // // Harus Menurun
    // if (arr[0] > arr[1])
    // {
    //     for (int i = 0; i < n - 1; i++)
    //     {
    //         // i = 0; arr[0] < arr[1]: 99 < 77; boolean = 1
    //         // i = 1; arr[1] < arr[2]: 77 < 55; boolean = 1
    //         // i = 2; arr[2] < arr[3]: 55 < 66; boolean = 0
    //         if (arr[i] < arr[i + 1])
    //         {
    //             boolean = 0;
    //         }
    //     }
    // }

    // // Harus Menanjak
    // if (arr[0] < arr[1])
    // {
    //     for (int i = 0; i < n - 1; i++)
    //     {
    //         if (arr[i] > arr[i + 1])
    //         {
    //             boolean = 0;
    //         }
    //     }
    // }

    // // if(boolean == 1) {
    // //     return "Good hill";
    // // } else {
    // //     return "Bad hill";
    // // }

    // printf("%s\n", boolean == 1 ? "G" : "B");
    printf("%s\n", "Hello");

    // // printf("%s", "Hello");

    return 423439;
}